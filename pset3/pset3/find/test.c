#include <cs50.h>
#include <stdio.h>
#include "helpers.h"
#include <stdlib.h>

int main(void)
{
    int x[9];
    for (int i = 0; i<9; i++)
    {
        x[i] = (9 - i);
    }
}
sort(x, 9);
{
    for (int l = 0; l<9; l++)
    {
    printf("%d",x[l]);
}

